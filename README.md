# images-uni-site

For university site for uploading images. 

---

# Installation: 

For the first time:

```bash
npm install
npm start
```

After you can just:

```bash
npm start
```

Also in `.env` file must be:

```.env
DB_URL= 
```
